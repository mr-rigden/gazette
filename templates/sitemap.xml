<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for post in posts %}
   <url>
      <loc>{{site.baseURL}}/{{ post.meta.slug }}</loc> 
      <lastmod>{{ post.meta.datetime.strftime('%Y-%m-%d') }}</lastmod>
   </url>
   {% endfor %}
</urlset>